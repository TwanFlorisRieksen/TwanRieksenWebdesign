# Rieksen Webstudio — static + JSON + Decap CMS (GitHub OAuth via Netlify)

Deze site is static-only. Content staat in `content/*.json`.
Decap CMS zit in `/admin`.

BELANGRIJK: vervang in `admin/config.yml` de repo: `REPLACE_OWNER/REPLACE_REPO`.
